@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityUser>

<h2>List of users</h2>
<table class="table">
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>
            <td>
                <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-userid="@user.Id">Acces rights</a>
            </td>
        </tr>
    }
</table>